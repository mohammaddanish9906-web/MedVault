workflows:
  android-workflow:
    name: Android Build
    max_build_duration: 60
    environment:
      node: latest
      java: 17
    scripts:
      - npm install
      - npm run build
      - npx cap sync android
      - cd android
      - ./gradlew assembleDebug
    artifacts:
      - android/app/build/outputs/**/*.apk
